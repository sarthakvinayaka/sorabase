import { AudioUploadWorkspace } from "@/components/AudioUploadWorkspace";

export default function UploadPage() {
  return (
    <div className="min-h-screen bg-zinc-100 text-zinc-900">
      <AudioUploadWorkspace />
    </div>
  );
}
